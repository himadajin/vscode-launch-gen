{
  "name": "Benchmark",
  "extends": "cpp",
  "enabled": true,
  "config": {
    "args": ["--benchmark", "--iterations", "1000"],
    "environment": [
      {"name": "BENCHMARK_MODE", "value": "1"}
    ]
  }
}